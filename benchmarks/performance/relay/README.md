# Relay Benchmark

This benchmark is used to measure the performance of the relay communication backends.

## Usage

```bash
python benchmark_relay.py
```

## Results

The results are saved in the `results` directory.
