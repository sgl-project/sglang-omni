services:
  ci-scheduler:
    build:
      context: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      DATABASE_PATH: /data/ci-scheduler.sqlite3
      WORKFLOWS_DIR: /app/workflows
      SCHEDULER_ROOT_WORKFLOWS: omni-ci.yaml
      HOST: 0.0.0.0
      PORT: 8080
    volumes:
      - scheduler-data:/data
      - ../.github/workflows:/app/workflows:ro
      # Optional: mount a private key file and set GITHUB_PRIVATE_KEY_PATH in .env.
      # - ./private-key.pem:/run/secrets/github-app.pem:ro

volumes:
  scheduler-data:
