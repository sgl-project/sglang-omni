name: PR Test (Examples)

on:
  workflow_dispatch:

concurrency:
  group: pr-test-examples-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  actions: read
  pull-requests: read
  issues: read

jobs:
  unit-test:
    name: unit-test
    # CI gate — runs on workflow_dispatch, or on non-draft PRs carrying the `run-ci` label.
    # Keep in sync with the same gate in test.yaml / test-qwen3-omni-ci.yaml / test-s2pro-ci.yaml.
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (!github.event.pull_request.draft &&
       contains(github.event.pull_request.labels.*.name, 'run-ci'))
    uses: ./.github/workflows/_omni-priority-stage.yaml
    with:
      stage-name: unit-test
      job-timeout-minutes: "60"
      run-timeout-minutes: "30"
      container-options: --gpus all --shm-size=2g --rm -v /dev/shm
      setup-action: "false"
      pre-run-script: |
        if [ -d /github/home/omni ] && [ ! -z "$(ls -A /github/home/omni/)" ]; then
          cp -p -r /github/home/omni ./
        fi

        rm -rf /github/home/.cache/flashinfer

        if [ -d omni ] && ! omni/bin/python -c "import torch" 2>/dev/null; then
          echo "Cached venv is corrupted, removing..."
          rm -rf omni
        fi
        if [ ! -d omni ]; then
          uv venv omni -p 3.11
        fi
        source omni/bin/activate
        uv pip install -v -e .

        bash .github/scripts/delete_gpu_process.sh
      run-script: |
        source omni/bin/activate
        export PYTHONPATH=$PWD
        export HF_ENDPOINT=https://hf-mirror.com
        bash .github/scripts/run_examples.sh
      save-cache-script: |
        cp -p -r omni /github/home/
