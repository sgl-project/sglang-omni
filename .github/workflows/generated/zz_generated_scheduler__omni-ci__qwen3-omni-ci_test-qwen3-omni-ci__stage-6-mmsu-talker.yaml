# Code generated by ci-scheduler workflow-gen. DO NOT EDIT.
# Source: .github/workflows/omni-ci.yaml
# Source job: stage-6-mmsu-talker
# Generator version: 1
# Source hash: 2a2d3fa1cc693a3d

name: Scheduler / Omni CI / stage 6 - MMSU Talker
run-name: scheduler dispatch-${{ inputs.dispatch_id }} queue-item-${{ inputs.queue_item_id
  }}
'on':
  workflow_dispatch:
    inputs:
      queue_item_id:
        required: true
        type: string
      dispatch_id:
        required: true
        type: string
      pr_number:
        required: true
        type: string
      head_sha:
        required: true
        type: string
      attempt:
        required: true
        type: string
      scheduler_context:
        required: true
        type: string
      source_hash:
        required: true
        type: string
permissions:
  contents: read
  checks: read
  pull-requests: read
env:
  OMNI_CI_HOME: "${{ fromJSON(inputs.scheduler_context).source_event.event_name ==\
    \ 'pull_request'\n    && format('/data/omni-ci/pr-{0}', fromJSON(inputs.scheduler_context).source_event.number)\n\
    \    || format('/data/omni-ci/run-{0}', github.run_id) }}"
  TTS_MODEL_PATH: bosonai/higgs-tts-3-4b
  MOSS_TTS_MODEL_PATH: OpenMOSS-Team/MOSS-TTS-Local-Transformer-v1.5
  QWEN3_ASR_MODEL_PATH: Qwen/Qwen3-ASR-1.7B
  MOSS_TRANSCRIBE_DIARIZE_MODEL_PATH: OpenMOSS-Team/MOSS-Transcribe-Diarize
jobs:
  run-stage:
    name: stage 6 - MMSU Talker
    runs-on:
    - self-hosted
    - h100
    timeout-minutes: 15
    container:
      image: crpi-n6adu6llixz83q37.cn-hangzhou.personal.cr.aliyuncs.com/hongccc/sglang-omni:dev
      options: --rm -e NVIDIA_VISIBLE_DEVICES --cap-add=SYS_PTRACE -v /dev/shm:/dev/shm
        -v /data/omni-ci:/data/omni-ci -v /data/cache/huggingface:/github/home/.cache/huggingface
        -v /data/cache/huggingface:/root/.cache/huggingface
    steps:
    - name: Checkout trusted orchestration
      uses: actions/checkout@v4
      with:
        ref: ${{ github.event.repository.default_branch }}
        path: trusted-orchestration
        sparse-checkout: ci-scheduler
    - name: Checkout PR source
      uses: actions/checkout@v4
      with:
        ref: ${{ inputs.head_sha }}
    - name: Checkout code
      uses: actions/checkout@v4
    - uses: ./.github/actions/omni-setup
      with:
        venv-name: omni
    - name: Run MMSU Talker CI (WER + speed)
      shell: bash
      run: "source omni/bin/activate\nexport PYTHONPATH=$PWD\nbash .github/scripts/run_flaky_pytest.sh\
        \ \\\n  pytest tests/test_model/test_qwen3_omni_mmsu_talker_ci.py -v -s -x\n"
      env:
        HF_ENDPOINT: https://huggingface.co
        TORCHINDUCTOR_CACHE_DIR: ${{ env.OMNI_CI_HOME }}/.torchinductor
    - name: Post-stage cleanup
      if: always()
      uses: ./.github/actions/omni-post-stage
      with:
        stage-label: MMSU Talker (WER + speed)
        artifact-path-globs: '*/mmsu_audio*/mmsu_results.json

          '
        artifact-upload-name: qwen3-omni-mmsu-talker-results
        artifact-upload-path: /tmp/**/mmsu_audio*/mmsu_results.json
    - name: Publish scheduler outputs
      if: always()
      shell: bash
      env:
        SCHEDULER_OUTPUT_NAMES: '[]'
      run: "set -euo pipefail\npython - <<'PY'\nimport json, os, pathlib\nnames =\
        \ json.loads(os.environ['SCHEDULER_OUTPUT_NAMES'])\noutputs = {\n    name:\
        \ os.environ.get(f'SCHEDULER_OUTPUT_{index}', '')\n    for index, name in\
        \ enumerate(names)\n}\npath = pathlib.Path('scheduler-outputs.json')\npath.write_text(json.dumps({'version':\
        \ 1, 'outputs': outputs}, sort_keys=True))\nprint(f'Wrote {path} with {len(outputs)}\
        \ outputs')\nPY"
    - name: Upload scheduler outputs artifact
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: ${{ inputs.dispatch_id }}
        path: scheduler-outputs.json
        if-no-files-found: ignore
