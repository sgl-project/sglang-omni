# Code generated by ci-scheduler workflow-gen. DO NOT EDIT.
# Source: .github/workflows/omni-ci.yaml
# Source job: setup
# Generator version: 1
# Source hash: 2a2d3fa1cc693a3d

name: Scheduler / Omni CI / setup - omni venv
run-name: scheduler dispatch-${{ inputs.dispatch_id }} queue-item-${{ inputs.queue_item_id
  }}
'on':
  workflow_dispatch:
    inputs:
      queue_item_id:
        required: true
        type: string
      dispatch_id:
        required: true
        type: string
      pr_number:
        required: true
        type: string
      head_sha:
        required: true
        type: string
      attempt:
        required: true
        type: string
      scheduler_context:
        required: true
        type: string
      source_hash:
        required: true
        type: string
permissions:
  contents: read
  checks: read
  pull-requests: read
env:
  OMNI_CI_HOME: "${{ fromJSON(inputs.scheduler_context).source_event.event_name ==\
    \ 'pull_request'\n    && format('/data/omni-ci/pr-{0}', fromJSON(inputs.scheduler_context).source_event.number)\n\
    \    || format('/data/omni-ci/run-{0}', github.run_id) }}"
  TTS_MODEL_PATH: bosonai/higgs-tts-3-4b
  MOSS_TTS_MODEL_PATH: OpenMOSS-Team/MOSS-TTS-Local-Transformer-v1.5
  QWEN3_ASR_MODEL_PATH: Qwen/Qwen3-ASR-1.7B
  MOSS_TRANSCRIBE_DIARIZE_MODEL_PATH: OpenMOSS-Team/MOSS-Transcribe-Diarize
jobs:
  run-stage:
    name: setup - omni venv
    runs-on:
    - self-hosted
    - h100
    timeout-minutes: 30
    container:
      image: crpi-n6adu6llixz83q37.cn-hangzhou.personal.cr.aliyuncs.com/hongccc/sglang-omni:dev
      options: --rm -e NVIDIA_VISIBLE_DEVICES -v /dev/shm:/dev/shm -v /data/omni-ci:/data/omni-ci
        -v /data/cache/huggingface:/github/home/.cache/huggingface -v /data/cache/huggingface:/root/.cache/huggingface
    steps:
    - name: Checkout trusted orchestration
      uses: actions/checkout@v4
      with:
        ref: ${{ github.event.repository.default_branch }}
        path: trusted-orchestration
        sparse-checkout: ci-scheduler
    - name: Checkout PR source
      uses: actions/checkout@v4
      with:
        ref: ${{ inputs.head_sha }}
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Assess CI environment completeness
      id: env_state
      shell: bash
      run: "echo \"OMNI_CI_HOME=${OMNI_CI_HOME}\"\nif bash .github/scripts/validate_omni_env_reusable.sh\
        \ omni; then\n  echo \"action=reuse\" >> \"${GITHUB_OUTPUT}\"\n  echo \"Existing\
        \ environment passes reuse gate; will refresh editable install.\"\nelse\n\
        \  echo \"action=rebuild\" >> \"${GITHUB_OUTPUT}\"\n  echo \"Environment missing\
        \ or drifted; will reconcile or rebuild.\"\nfi\n"
    - uses: ./.github/actions/omni-setup
      with:
        venv-name: omni
        install-deps: 'true'
    - uses: ./.github/actions/omni-save-cache
      with:
        venv-name: omni
    - name: Ensure CI model weights
      shell: bash
      env:
        HF_ENDPOINT: https://huggingface.co
        HF_TOKEN: ${{ secrets.HF_TOKEN }}
      run: "bash .github/scripts/ensure_hf_models.sh omni \\\n  Qwen/Qwen3-Omni-30B-A3B-Instruct\
        \ \\\n  marksverdhei/Qwen3-Omni-30B-A3B-FP8 \\\n  \"${QWEN3_ASR_MODEL_PATH}\"\
        \ \\\n  \"${MOSS_TRANSCRIBE_DIARIZE_MODEL_PATH}\" \\\n  \"${TTS_MODEL_PATH}\"\
        \ \\\n  \"${MOSS_TTS_MODEL_PATH}\"\n"
    - name: Mark PR environment complete
      shell: bash
      run: bash .github/scripts/mark_omni_env_complete.sh omni
    - name: Verify setup completion marker
      shell: bash
      run: bash .github/scripts/validate_omni_env_complete.sh omni
    - name: Publish scheduler outputs
      if: always()
      shell: bash
      env:
        SCHEDULER_OUTPUT_NAMES: '[]'
      run: "set -euo pipefail\npython - <<'PY'\nimport json, os, pathlib\nnames =\
        \ json.loads(os.environ['SCHEDULER_OUTPUT_NAMES'])\noutputs = {\n    name:\
        \ os.environ.get(f'SCHEDULER_OUTPUT_{index}', '')\n    for index, name in\
        \ enumerate(names)\n}\npath = pathlib.Path('scheduler-outputs.json')\npath.write_text(json.dumps({'version':\
        \ 1, 'outputs': outputs}, sort_keys=True))\nprint(f'Wrote {path} with {len(outputs)}\
        \ outputs')\nPY"
    - name: Upload scheduler outputs artifact
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: ${{ inputs.dispatch_id }}
        path: scheduler-outputs.json
        if-no-files-found: ignore
