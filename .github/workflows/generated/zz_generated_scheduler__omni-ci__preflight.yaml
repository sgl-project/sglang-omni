# Code generated by ci-scheduler workflow-gen. DO NOT EDIT.
# Source: .github/workflows/omni-ci.yaml
# Source job: preflight
# Generator version: 1
# Source hash: 2a2d3fa1cc693a3d

name: Scheduler / Omni CI / omni-ci-gate
run-name: scheduler dispatch-${{ inputs.dispatch_id }} queue-item-${{ inputs.queue_item_id
  }}
'on':
  workflow_dispatch:
    inputs:
      queue_item_id:
        required: true
        type: string
      dispatch_id:
        required: true
        type: string
      pr_number:
        required: true
        type: string
      head_sha:
        required: true
        type: string
      attempt:
        required: true
        type: string
      scheduler_context:
        required: true
        type: string
      source_hash:
        required: true
        type: string
permissions:
  contents: read
  checks: read
  pull-requests: read
env:
  OMNI_CI_HOME: "${{ fromJSON(inputs.scheduler_context).source_event.event_name ==\
    \ 'pull_request'\n    && format('/data/omni-ci/pr-{0}', fromJSON(inputs.scheduler_context).source_event.number)\n\
    \    || format('/data/omni-ci/run-{0}', github.run_id) }}"
  TTS_MODEL_PATH: bosonai/higgs-tts-3-4b
  MOSS_TTS_MODEL_PATH: OpenMOSS-Team/MOSS-TTS-Local-Transformer-v1.5
  QWEN3_ASR_MODEL_PATH: Qwen/Qwen3-ASR-1.7B
  MOSS_TRANSCRIBE_DIARIZE_MODEL_PATH: OpenMOSS-Team/MOSS-Transcribe-Diarize
jobs:
  run-stage:
    name: omni-ci-gate
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      labels: ${{ steps.pr.outputs.labels || '[]' }}
    steps:
    - name: Checkout trusted orchestration
      uses: actions/checkout@v4
      with:
        ref: ${{ github.event.repository.default_branch }}
        path: trusted-orchestration
        sparse-checkout: ci-scheduler
    - name: Checkout PR source
      uses: actions/checkout@v4
      with:
        ref: ${{ inputs.head_sha }}
    - name: Fetch latest PR gate state
      if: ${{ fromJSON(inputs.scheduler_context).source_event.event_name == 'pull_request'
        }}
      id: pr
      uses: actions/github-script@v7
      with:
        github-token: ${{ github.token }}
        script: "// Reruns reuse the original pull_request event payload, so fetch\n\
          // live labels/draft state after /tag-and-rerun-ci adds run-ci.\nconst pr\
          \ = await github.rest.pulls.get({\n  owner: context.repo.owner,\n  repo:\
          \ context.repo.repo,\n  pull_number: context.issue.number,\n});\ncore.setOutput(\"\
          labels\", JSON.stringify(pr.data.labels.map((label) => label.name)));\n\
          core.setOutput(\"draft\", pr.data.draft ? \"true\" : \"false\");\n"
    - name: Log PR gate state
      if: ${{ fromJSON(inputs.scheduler_context).source_event.event_name == 'pull_request'
        }}
      run: 'echo "PR labels: ${{ steps.pr.outputs.labels }}"

        echo "PR draft: ${{ steps.pr.outputs.draft }}"

        '
    - name: Block draft PR
      if: ${{ fromJSON(inputs.scheduler_context).source_event.event_name == 'pull_request'
        && fromJson(steps.pr.outputs.draft) }}
      run: 'echo "PR is draft. Blocking Omni CI."

        exit 1

        '
    - name: Require run-ci label
      if: ${{ fromJSON(inputs.scheduler_context).source_event.event_name == 'pull_request'
        }}
      run: "if [[ \"${{ contains(fromJson(steps.pr.outputs.labels), 'run-ci') }}\"\
        \ == \"false\" ]]; then\n  echo \"Missing required label 'run-ci'. Add the\
        \ label or use /tag-and-rerun-ci to opt into Omni CI.\"\n  exit 1\nfi\n"
    - name: Checkout workflow scripts
      if: ${{ fromJSON(inputs.scheduler_context).source_event.event_name == 'pull_request'
        }}
      uses: actions/checkout@v4
      with:
        ref: ${{ fromJSON(inputs.scheduler_context).source_event.head.sha }}
    - name: Check PR lint and mergeability
      if: ${{ fromJSON(inputs.scheduler_context).source_event.event_name == 'pull_request'
        }}
      env:
        GITHUB_TOKEN: ${{ github.token }}
      run: python .github/scripts/pr_ci_gate.py
    - name: Manual dispatch preflight
      if: ${{ fromJSON(inputs.scheduler_context).source_event.event_name == 'workflow_dispatch'
        }}
      run: echo "Manual workflow_dispatch bypasses PR lint/mergeability gate."
    - name: Publish scheduler outputs
      if: always()
      shell: bash
      env:
        SCHEDULER_OUTPUT_NAMES: '["labels"]'
        SCHEDULER_OUTPUT_0: ${{ steps.pr.outputs.labels || '[]' }}
      run: "set -euo pipefail\npython - <<'PY'\nimport json, os, pathlib\nnames =\
        \ json.loads(os.environ['SCHEDULER_OUTPUT_NAMES'])\noutputs = {\n    name:\
        \ os.environ.get(f'SCHEDULER_OUTPUT_{index}', '')\n    for index, name in\
        \ enumerate(names)\n}\npath = pathlib.Path('scheduler-outputs.json')\npath.write_text(json.dumps({'version':\
        \ 1, 'outputs': outputs}, sort_keys=True))\nprint(f'Wrote {path} with {len(outputs)}\
        \ outputs')\nPY"
    - name: Upload scheduler outputs artifact
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: ${{ inputs.dispatch_id }}
        path: scheduler-outputs.json
        if-no-files-found: ignore
